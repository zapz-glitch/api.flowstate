{
  "name": "flowstate-api",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy": "turbo deploy",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:generate": "turbo db:generate",
    "db:migrate:local": "turbo db:migrate:local",
    "db:migrate:remote": "turbo db:migrate:remote"
  },
  "devDependencies": {
    "@esbuild/darwin-arm64": "^0.27.3",
    "@types/bcryptjs": "^2.4.6",
    "bcryptjs": "^3.0.3",
    "turbo": "^2.3.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "npm@10.2.0",
  "dependencies": {
    "@noble/hashes": "^2.0.1"
  }
}
